# spring boot props
spring:
  application:
    name: gcs-streaming

# GCS configuration
gcs:
  input:
    bucket: test_bucket
    path: /input/
    fileExtension: .json
  output:
    bucket: test_bucket
    path: /output/